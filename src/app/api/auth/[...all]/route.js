export async function GET() {
  return Response.json({
    message: "Demo auth is handled by localStorage in this project.",
  });
}

export async function POST() {
  return Response.json({
    message: "Demo auth is handled by localStorage in this project.",
  });
}